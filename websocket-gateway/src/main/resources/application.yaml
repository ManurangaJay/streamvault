server:
  port: 8083

spring:
  application:
    name: websocket-gateway

  data:
    redis:
      host: localhost
      port: 6379

  cloud:
    function:
      definition: accountEventConsumer;alertEventConsumer
    stream:
      kafka:
        binder:
          brokers: localhost:9092
          auto-create-topics: false
      bindings:
        accountEventConsumer-in-0:
          destination: account.events
          group: ws-gateway-group
          content-type: application/json
          
        alertEventConsumer-in-0:
          destination: alert.events
          group: ws-gateway-group
          content-type: application/json

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus
  metrics:
    tags:
      application: ${spring.application.name}

jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
